<h3>Welcome!</h3>

<ul>
  <% @post.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post) %> (<%= link_to "edit", edit_post_path(post)%>)
      <%= post.user.username if post.user %>
      <br/>
      

    </li>
  <% end %> 
</ul>
